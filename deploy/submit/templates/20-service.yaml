kind: Service
metadata:
  annotations:
    prometheus.io/scrape: 'true'
  name: "{{ default "submission-ui" .Values.service.name }}"
  labels:
    subsystem: "{{ .Values.labels.subsystem }}"
    container: "{{ default "submission-ui" .Values.deployment.name }}"
    service-group: ui
    log-style: uwsgi
    env: "{{ .Values.namespace }}"
  namespace: "{{ .Values.namespace }}"
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8000
  selector:
    subsystem: "{{ .Values.labels.subsystem }}"
    container: "{{ default "submission-ui" .Values.deployment.name }}"
